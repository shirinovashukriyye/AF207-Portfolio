import { Typography, Container } from '@mui/material';

const Home = () => {
  return (
    <Container>
      <Typography variant="h4" sx={{ mt: 4 }}>
        Welcome to MyStore
      </Typography>
    </Container>
  );
};

export default Home;
